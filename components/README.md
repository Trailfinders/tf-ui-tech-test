# components/

Export your reusable component designs here.

Show each component with at least 2 states side by side, e.g.:
- `task-card.png` (default + completed)
- `button.png` (default + pressed)
- `input.png` (empty + filled)
